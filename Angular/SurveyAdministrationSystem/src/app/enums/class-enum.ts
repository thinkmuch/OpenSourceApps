export enum Alerts {
    Danger = "alert-danger"
}

export enum AnswerType {
    SingleAnswer = 1,
    FreeText = 2,
    MultipleChoises = 3
}

export enum Status {
    Inactive = 1,
    Active = 2
}

export enum SelectArea {
    Default = 0,
    SelectArea = 1,
    AreasNotExist = 2,
    AreaName = 3
}